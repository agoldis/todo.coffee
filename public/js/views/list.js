// Generated by CoffeeScript 1.9.1
(function() {
  define(['backbone', 'underscore', 'views/item'], function(Backbone, _, ItemView) {
    return Backbone.View.extend({
      tagName: 'ul',
      initialize: function() {
        this.collection.on('add', this.addItem, this);
        this.collection.on('remove', this.removeItem, this);
        return this.itemViews = [];
      },
      addItem: function(item) {
        var newView;
        newView = new ItemView({
          model: item
        });
        this.itemViews.push(newView);
        return this.$el.append(newView.render().el);
      },
      removeItem: function(item) {
        var i;
        i = _.indexOf(this.itemViews, item);
        return this.itemViews.splice(i, 1);
      },
      render: function() {
        var j, len, ref, view;
        this.$el.html('');
        ref = this.itemViews;
        for (j = 0, len = ref.length; j < len; j++) {
          view = ref[j];
          this.$el.append(view.render().el);
        }
        return this;
      }
    });
  });

}).call(this);
