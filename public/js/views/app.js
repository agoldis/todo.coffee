// Generated by CoffeeScript 1.9.1
(function() {
  define(['mr', 'underscore', 'collections/list', 'views/list', 'views/controller', 'models/item', 'text!templates/layout.html'], function(Mr, _, TodoCollection, ListView, ControllerView, ItemModel, layoutTpl) {
    return Mr.LayoutView.extend({
      template: _.template(layoutTpl),
      regions: {
        controls: '#app-ctrl',
        list: '#app-items-list'
      },
      initialize: function() {
        return this.collection = new TodoCollection();
      },
      onRender: function() {
        this.showChildView('controls', new ControllerView({
          collection: this.collection
        }));
        this.showChildView('list', new ListView({
          collection: this.collection
        }));
        return this.collection.fetch();
      }
    });
  });

}).call(this);

//# sourceMappingURL=app.js.map
